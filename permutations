#include <cs50.h>
#include <stdio.h>
#include <stdlib.h>
#include <math.h>

void permute(int startNum);

int base = 5;
int currPerm[] = {0, 0, 0, 0, 0};
int currIndex = 0;


int main(void) {
    
    for (int i = 0; i < base; i++)
    {
        for (int a = 0; a < (base-i) a++) {
            if (i == 0) {
                currPerm[i] = i;
            } else {
                currPerm[i] = a;
            }
        }
    }
    
}


// permute function
void permute(int startNum) {
    
    printf("%i", startNum);
    
}
