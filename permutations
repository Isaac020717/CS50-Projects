#include <cs50.h>
#include <stdio.h>
#include <stdlib.h>
#include <math.h>


int main(void) {
    
    int base = 5;
    
    for (int i = 1; i <= base; i++)
    {
        permute(i);
    }
    
}


// permute function
void permute(int startNum) {
    
}

